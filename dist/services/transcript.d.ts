import type { Transcript } from "../types.js";
export declare function fetchTranscript(videoIdOrUrl: string, language?: string): Promise<Transcript>;
export declare function listAvailableLanguages(videoIdOrUrl: string): Promise<{
    code: string;
    name: string;
    isAutoGenerated: boolean;
}[]>;
